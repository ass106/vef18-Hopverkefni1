* {
    padding: 0;
    margin: 0;
}

*, *:before, *:after {
    box-sizing: inherit;
}

html {
    box-sizing: border-box;
}
.imgs {
    display: flex;
    max-width: 50%;
    justify-content: space-between;
    flex-wrap: wrap;
}

body {
    background-color: pink;
}

